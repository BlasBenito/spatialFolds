% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deduplicate_folds.R
\name{deduplicate_folds}
\alias{deduplicate_folds}
\title{Remove Redundant Folds Based on Correlation}
\usage{
deduplicate_folds(folds, max_cor = 0.9, ...)
}
\arguments{
\item{folds}{(required, data.frame) Output from \code{spatial_folds()} containing
logical vector columns. Each column represents a fold with TRUE for training
and FALSE for testing samples.}

\item{max_cor}{(optional, numeric) Maximum allowed correlation between folds.
Folds with correlation above this threshold are considered redundant and
removed. Must be between 0 and 1. Default: 0.9}

\item{...}{Internal parameters passed from parent functions.}
}
\value{
Data frame with same structure as input, but with redundant columns
removed. Column names and data types are preserved.
}
\description{
Reduces redundancy in spatial folds by removing highly correlated folds
within each method-fraction group. Uses \code{collinear::collinear_select()} to
identify and keep only folds that are sufficiently different from each other.
}
\details{
This function addresses the issue where spatial folding methods (especially
"contiguous") can produce similar folds when center points are close to each
other. Redundant folds add computational overhead without contributing unique
information to cross-validation.

The algorithm works as follows:
\enumerate{
\item \strong{Parse column names} to identify method-fraction groups (e.g., "contiguous_0.75")
\item \strong{For each group}:
\itemize{
\item Extract columns belonging to that group
\item Convert logical vectors to numeric (TRUE=1, FALSE=0)
\item Apply \code{collinear::collinear_select()} to find uncorrelated columns
}
\item \strong{Combine} selected columns from all groups
\item \strong{Return} reduced data frame with original column order preserved
}

Deduplication is performed within groups (not across groups) because different
methods and training fractions produce structurally different folds that should
not be compared directly.
}
\examples{
# Generate folds with potential redundancy
folds <- spatial_folds(
  df = xy_sf,
  methods = "random",
  training_fraction = 0.75,
  repetitions = 50,
  seed = 123
)

# Remove redundant folds (correlation > 0.9)
folds_unique <- deduplicate_folds(folds)

# More aggressive deduplication
folds_strict <- deduplicate_folds(folds, max_cor = 0.8)

}
\seealso{
Other primary_functions: 
\code{\link{spatial_folds}()}
}
\concept{primary_functions}
