% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{method_contiguous_planar}
\alias{method_contiguous_planar}
\title{(C++) Generate Contiguous Training Fold Using Planar Geometry}
\usage{
method_contiguous_planar(xy, center, step_x, step_y, target)
}
\arguments{
\item{xy}{(required, numeric matrix) Two columns matrix with the locations
to arrange. The first column is interpreted as "x" (longitude) and the
second as "y" (latitude). Default: \code{NULL}}

\item{center}{(required, integer) Index of the training fold center (1-based
indexing as in R). Default: \code{NULL}}

\item{step_x}{(required, numeric) Rectangle growth increment along the
x-axis. Must be in the same units as \code{xy}. Default: \code{NULL}}

\item{step_y}{(required, numeric) Rectangle growth increment along the
y-axis. Must be in the same units as \code{xy}. Default: \code{NULL}}

\item{target}{(optional, integer) Number of records to include in the
training fold. Default: \code{NULL}.}
}
\value{
logical vector with length equal to nrow(xy), where TRUE indicates a
record is in the training fold and FALSE indicates it is in the testing
fold.
}
\description{
Optimized implementation using binary search to find rectangle
dimensions. Suitable for local to subcontinental scale data where planar
geometry is a good approximation. For global data near the dateline or
poles, use \code{method_contiguous_spherical()} instead.
}
\details{
This function uses binary search to efficiently find the rectangle size:
\enumerate{
\item Determines the maximum possible rectangle scale from data extent
\item Binary searches for scale factor that yields target count
\item Each iteration counts points in rectangle (O(n))
\item Total iterations: O(log(max_scale)) typically 10-15
\item Returns logical vector for final rectangle
}

This maintains rectangle shape and produces similar results to the original
implementation while being much faster through reduced iterations.

Complexity: O(n log iterations) where iterations = 10-15
Performance: Typically 15-20x faster than R version on large datasets
}
\examples{
training <- method_contiguous_planar(
  xy = xy_matrix,
  center = 1, #first record in xy_matrix
  step_x = 0.4,
  step_y = 0.1,
  target = 15000
)

spatial_fold_plot(
  df = xy_matrix,
  training_fold = training
)
}
\seealso{
Other contiguous: 
\code{\link{method_contiguous_spherical}()}
}
\concept{contiguous}
