% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/training_fold.R
\name{training_fold}
\alias{training_fold}
\title{Internal function to generate a training fold}
\usage{
training_fold(
  xy = NULL,
  center = NULL,
  step_x = NULL,
  step_y = NULL,
  training_fraction = 0.8
)
}
\arguments{
\item{xy}{(required, matrix) Two columns matrix with the locations to arrange. The first column is interpreted as "x" (longitude) and the second as "y" (latitude). Default: \code{NULL}}

\item{center}{(required, integer) Index of the training fold center. Default: \code{NULL}}

\item{step_x}{(optional, numeric) Rectangle growth increment along the x-axis. Must be in the same units as \code{xy}. Default: \code{NULL}}

\item{step_y}{(optional, numeric) Rectangle growth increment along the y-axis. Must be in the same units as \code{xy}. Default: \code{NULL}}

\item{training_fraction}{(optional, numeric) Fraction of records to include in the training fold. Default: \code{0.8}.}
}
\value{
logical vector: with length equal to \code{nrow(xy)}, where \code{TRUE} indicates a record is in the training fold and \code{FALSE} indicates it is in the testing fold. The vector is ordered by row position in \code{xy}.
}
\description{
Taking a focal point as reference, it grows a rectangular area until a specified fraction of all records falls inside. Records inside the rectangular area are the training set, and records outside are the testing set. This method aims to preserve the spatial structure of the training data.
}
\details{
This function creates spatially independent training and testing folds for spatial cross-validation. The algorithm works as follows:
\enumerate{
\item Starts with a small rectangular fold centered on the focal point (\code{center})
\item Grows the fold incrementally by \code{step_x} and \code{step_y}
\item Continues growing until the fold contains the desired number of records (\verb{training_fraction * total records})
\item Assigns records inside the fold to training and records outside to testing
}
}
\examples{
training <- training_fold(
  xy = xy_matrix,
  center = 1,
  step_x = 0.4,
  step_y = 0.1,
  training_fraction = 0.50
)

training_fold_plot(
  xy = xy_matrix,
  center = 1,
  training_fold = training
)
}
