% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/training_mask.R
\name{training_mask}
\alias{training_mask}
\title{Generate a single training mask using a specified method}
\usage{
training_mask(
  xy,
  method = c("contiguous", "random", "blocks"),
  spherical = FALSE,
  ...
)
}
\arguments{
\item{xy}{(required, numeric matrix) Two-column matrix with x (longitude) and
y (latitude) coordinates.}

\item{method}{(required, character) Method to use: "contiguous", "random", or
"blocks". Default: \code{"contiguous"}}

\item{spherical}{(optional, logical) If TRUE, uses spherical geometry for the
contiguous method. Required for global data near dateline or poles. If
FALSE, uses faster planar geometry suitable for local/regional data.
Default: \code{FALSE}}

\item{...}{Method-specific arguments:
\itemize{
\item For "contiguous": center (int), step_x (double), step_y (double), target
(double)
\item For "random": seed (int), target (double)
\item For "blocks": block_id (integer vector), seed (int), target (double)
}}
}
\value{
Logical vector where TRUE = training samples, FALSE = testing samples
}
\description{
Thin R wrapper for C++ methods that generates a single training/testing split.
Intended to be called within a parallelized loop. Assumes all inputs are valid.
}
\details{
This function routes to the appropriate C++ method based on the method
parameter:
\enumerate{
\item \strong{method_contiguous_planar}: Creates contiguous training folds using
planar geometry
\itemize{
\item Grows rectangular fold until target count reached
\item Fast and suitable for local to subcontinental data
}
\item \strong{method_contiguous_spherical}: Creates contiguous training folds using
spherical geometry
\itemize{
\item Grows spherical cap until target count reached
\item Correctly handles dateline wrap-around and polar convergence
\item Required for global data near boundaries
}
\item \strong{method_random}: Random data splitting using modern C++ RNG
\itemize{
\item Uses Fisher-Yates shuffle algorithm
\item Reproducible with seed
}
\item \strong{method_blocks}: Selects entire grid blocks until target count reached
\itemize{
\item Requires pre-computed block_id vector
\item Entire blocks selected (may exceed target)
}
}
}
